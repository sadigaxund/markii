node_modules
dist
coverage
.vite
package-lock.json

# Orchestrator-owned prose; not subject to the code formatting gate (see
# CLAUDE.md: subagents must not edit these files).
DESIGN.md
CLAUDE.md
TODO.md

# Orchestrator-owned prose (long-form; not Prettier-managed).
docs/
TODO.md
# Audit artifacts: verbatim reports, kept as written; verdict folds into docs/security.md
PENTEST-REPORT-*.md
# conformance fixtures are byte-exact test data — formatters must not rewrite them
conformance/
# Mark documents: Prettier's markdown formatter breaks directive fences
# (a `:::` line becomes a table cell), corrupting the document
**/*.mk.md
